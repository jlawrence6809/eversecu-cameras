# Copy this file to .env, chmod it to 600, and fill in the password locally.
CAMERA_HOST=192.0.2.10
CAMERA_USERNAME=admin
CAMERA_NAME=yard

# Preferred on macOS: store the password in Keychain under this service name.
CAMERA_KEYCHAIN_SERVICE=eversecu-coyote-camera
# Fallback for non-macOS systems (avoid this when Keychain is available):
# CAMERA_PASSWORD=replace-me

# Detector tuning. The initial COCO model calls likely coyotes "dog".
DETECTION_INTERVAL_SECONDS=1.0
DETECTION_CONFIDENCE=0.35
EVENT_COOLDOWN_SECONDS=30
EVENT_RETENTION_DAYS=14
EVENT_OUTPUT_DIR=events

# Give the camera a quiet period after a broken long-lived RTSP session.
RECONNECT_INITIAL_SECONDS=30
RECONNECT_MAX_SECONDS=300

# A separate watchdog uses this heartbeat to detect stale frames or a dead worker.
HEALTH_FILE=state/health.json
